<br />
<div class="row">
	<div class="col-lg-12">
		<div class="panel panel-default">
			<div class="panel-heading">
				<h3><%= t('users.editing_user')%></h3>
			</div>
			<div class="panel-body">
				<div class="row">
					<%= render 'form' %>
				</div>
			</div>
		</div>
	</div>
</div>

<%= link_to t('general.buttons.show'), [:admin, @user], class: 'btn btn-default' %>
<%= link_to t('general.buttons.back'), admin_users_path, class: 'btn btn-default' %>
